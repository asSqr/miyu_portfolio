import React from 'react'

const worksTitle = 'Works'

const worksContent = `
鋭意製作中
`

export const Works = () => {
  return (
    <section>
      <h1>{worksTitle}</h1>
      <article>{worksContent}</article>
    </section>
  )
}
